import React from 'react';
import { render, screen } from '@testing-library/react';

import WheatherChart from '../WheatherChart';

describe('WheatherChart', () => {
    test('Render WheatherChart component', () => {
        render(<WheatherChart/>);
      // screen.debug();
      // findBy...
      // queryBy...
    })
  })